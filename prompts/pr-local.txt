Generate a GitHub pull request title and body for the following changes.

Format your response EXACTLY like this (no other format):
TITLE: <title here>
BODY:
<body here>

Example output:
TITLE: Add user authentication with JWT
BODY:
Adds login/signup endpoints and JWT-based session management.

- Add POST /login and POST /signup routes
- Add JWT token generation and validation
- Add auth middleware for protected routes
- Add password hashing with bcrypt

Rules:
- Keep the title under 72 characters.
- The body should have a one-line summary, then a bullet list of changes.
- Use markdown in the body.
- Do NOT wrap the output in code fences or backticks.
- Use REAL line breaks. Do NOT output literal \n characters.
- Pay attention to the Context line — if files are new, describe them as additions, not moves or refactors.